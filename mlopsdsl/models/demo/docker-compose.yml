services:
  api:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DB_HOST_OVERRIDE: host.docker.internal
